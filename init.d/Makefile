install:
	install -v -t /etc/init.d card proxy sigd suicide webservice
	update-rc.d card defaults
	update-rc.d proxy defaults
	update-rc.d sigd defaults
	update-rc.d suicide defaults
	update-rc.d webservice defaults
